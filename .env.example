# NourishOps backend — offline mode needs NO configuration (05 §3.1).
# Set these only to exercise the optional live-agent adapter (05 §3.2);
# the judged demo path runs fully offline with no provider key.
NOURISHOPS_AGENT_MODE=offline
# NOURISHOPS_LLM_PROVIDER=
# NOURISHOPS_LLM_API_KEY=
